<%= form_for (@post) do |f| %>
	<div class="field">
	<%= f.label :title %><br>
	<%= f.text_field :title %><br>
	</div>

	<div class="field">
	<%= f.label :body %><br>
	<%= f.text_area :body, size: "60x20" %><br>
	</div>

	<div class="actions">
	<%= f.submit %>
	</div>
<% end %>